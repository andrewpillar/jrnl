package usage

var Publish = `jrnl publish - Publish a post to a remote

Usage:

  jrnl publish [post] [options...]

Options:

  -d, --draft   Convert the Markdown to HTML, but don't copy to the remote
  -r, --remote  The remote to copy the post to

  --help  Display this usage message`
