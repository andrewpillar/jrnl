<!DOCTYPE HTML>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<title>{{.Title}} - {{.Site.Title}}</title>
	</head>
	<body>
		<ul>
			{{range .Posts}}
				<a href="{{.URL}}">{{.Title}}</a>
				<p>{{.Description}}</p>
			{{end}}
		</ul>
	</body>
</html>
